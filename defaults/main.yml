---
keepalived_templates:
  - src: check_script.sh.j2
    dest: /etc/keepalived/check_script.sh
    mode: "0755"
  - src: keepalived.conf.j2
    dest: /etc/keepalived/keepalived.conf
    mode: "0644"

# check_script.sh.j2 variables
keepalived_check_process: "keepalived"
